# 服务器端口（Render 会自动注入 PORT，无需手动设置）
PORT=3001

# JWT 密钥（生产环境请设置为随机长字符串）
JWT_SECRET=your_super_secret_key_change_this_in_production

# 数据库目录（Render 持久化磁盘路径，本地无需设置）
# DATA_DIR=/opt/render/project/src/data

# 环境
NODE_ENV=development
