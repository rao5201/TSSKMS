# Zeabur 部署配置
# Zeabur 会自动识别 Node.js 项目

# backend 服务配置
[services.backend]
name = "teahaixin-api"
region = "sin"  # 新加坡节点，延迟低
runtime = "node"
build_command = "npm install"
start_command = "npm start"

# 环境变量
[services.backend.env]
NODE_ENV = "production"
PORT = "3001"
